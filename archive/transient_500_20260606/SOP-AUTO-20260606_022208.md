# SOP-AUTO-20260606T021706 : Restore Operation Failure Triage on Azure SQL MI to SQL Server 2025

## Classification
- **Category**: RESTORE  
- **Severity**: P1  
- **Auto-Generated**: Yes  
- **Generated On**: 2026-06-06T02:17:06Z

## Problem Description
The automated restore operation of the `salesdb` database from Azure SQL Managed Instance backups stored in Azure Blob Storage failed with a server error (500) during the processing of the RESTORE FROM URL command on the target SQL Server 2025 instance hosted on the Windows VM (10.10.0.5). The error message indicated an internal server error on the Azure Blob Storage service or the SQL Server endpoint handling the restore.

## Root Cause Analysis
Considering the full architecture and error context, multiple factors may have contributed:

1. **Azure Blob Storage Service Issue**:  
   - The 500 server error signals an internal server problem from Azure Blob Storage or intermediate components.  
   - Possible transient issues or outages in the storage account `2276259blob`.

2. **SAS Token Issues**:  
   - Expired or improperly configured SAS token could cause authorization failures or interrupted connections, potentially triggering server errors.  
   - Incorrect permissions for the SAS token may prevent blob listing or file access.

3. **Blob Container or Path Mismatch**:  
   - Backup files are expected under container `backupfromazsqlmi`.  
   - If the target file name or path does not match the expected naming conventions, the RESTORE FROM URL could fail.  
   - The automation’s blob list discovery may have missed or misidentified the file.

4. **Network or Firewall Restrictions**:  
   - The Windows VM (10.10.0.5) must have network access to Azure Blob Storage endpoints over HTTPS port 443.  
   - Any firewall rules blocking outbound traffic to storage endpoints can cause connection failures raised as errors.

5. **SQL Server Side RESTORE Command Handling**:  
   - SQL Server 2025 instance must be properly configured with credentials (e.g., a SQL Server credential with SAS token) to access the blob.  
   - Incorrect or corrupted credentials could trigger server errors during restore.

6. **Automation and pyodbc Connectivity**:  
   - The Linux dev VM running automation via pyodbc remotely connects to SQL Server; network timeout or permissions on the SQL Server instance might indirectly cause operation failure.

## Resolution Steps
Follow these steps to diagnose and resolve the failure within this Azure cloud architecture:

1. **Verify Azure Blob Storage Service Health**  
   - Check Azure Service Health and the status of the storage account `2276259blob`.  
   - Use Azure Portal or CLI to confirm no ongoing incidents or degradations.

2. **Validate SAS Token**  
   - Confirm the SAS token used in the SQL Server credential is valid and not expired.  
   - Validate SAS token has permissions: `Read`, `List` on the blob container `backupfromazsqlmi`.  
   - Regenerate SAS token if near or past expiry and update SQL Server credential accordingly.

3. **Confirm Backup Blob Existence and Naming**  
   - From the Linux dev VM or Azure CLI, list blobs in `backupfromazsqlmi` container:  
     ```bash
     az storage blob list --container-name backupfromazsqlmi --account-name 2276259blob --output table
     ```  
   - Ensure the `.bak` file matching the `salesdb` backup exists and follows naming conventions expected by the restore automation.  
   - Check for subfolders or unexpected path prefixes that could cause mismatches.

4. **Test Network Access from Windows VM to Blob Storage**  
   - Log in to the Windows VM (10.10.0.5) and test network connectivity to blob endpoints, e.g.:  
     ```powershell
     Test-NetConnection -ComputerName 2276259blob.blob.core.windows.net -Port 443
     ```  
   - Check firewall rules that could block outbound HTTPS traffic.

5. **Validate SQL Server Credential Setup**  
   - On SQL Server instance, verify the credential for RESTORE is properly created with the current SAS token:  
     ```sql
     SELECT * FROM sys.credentials WHERE name = 'AzureBlobRestoreCredential';
     ```  
   - Recreate or update the credential with new SAS token if needed:  
     ```sql
     CREATE OR ALTER CREDENTIAL AzureBlobRestoreCredential  
     WITH IDENTITY = 'SHARED ACCESS SIGNATURE', SECRET = '<SAS-token-without-?'>;  
     ```

6. **Manual Restore Trial**  
   - Attempt to restore manually using RESTORE FROM URL to isolate automation issues:  
     ```sql
     RESTORE DATABASE salesdb  
     FROM URL = 'https://2276259blob.blob.core.windows.net/backupfromazsqlmi/salesdb_backup.bak'  
     WITH CREDENTIAL = 'AzureBlobRestoreCredential', REPLACE;  
     ```  
   - Capture detailed error messages for further troubleshooting.

7. **Check Automation Logs and pyodbc Errors**  
   - Review automation logs on Linux dev VM for network or permission errors.  
   - Validate pyodbc connection string and credentials to ensure no interruption.

8. **Retry Operation**  
   - After addressing above checks, retry the restore operation.

9. **Raise Azure Support Ticket if Issue Persists**  
   - Include request ID `c8b5c87a-823b-4ec3-8735-d3399aaad3a0` from error message in ticket  
   - Link: https://go.microsoft.com/fwlink/?linkid=2213926

## Prevention
- Implement monitoring and alerting on SAS token expiry to proactively renew.  
- Automate verification of blob existence and naming BEFORE restore attempts.  
- Schedule periodic network connectivity checks from Windows VM to storage endpoints.  
- Maintain runbooks for SQL Server credential updates with SAS tokens.  
- Incorporate retry logic with exponential backoff for transient Azure service errors.  
- Use Azure Resource Health alerts for storage account and network.

## Suggested Tool Improvement
- Enhance automation tool to validate SAS token expiry and regenerate tokens automatically.  
- Add pre-restore validation step that confirms blob file presence and accessibility before initiating RESTORE.  
- Implement detailed error logging and capture Azure request IDs for troubleshooting.  
- Integrate Azure SDK calls to verify Blob Storage health and permissions prior to restore commands.  
- Provide user-friendly translation of Azure 500 server errors with actionable recommendations in automation logs.