```markdown
# SOP-AUTO-2026-06-06T23:40:52 : Restore Operation Reject due to Database Name Mismatch

## Classification
- **Category**: RESTORE
- **Severity**: P2
- **Auto-Generated**: Yes
- **Generated On**: 2026-06-06T23:40:52

## Problem Description
A RESTORE operation for the database "EnterpriseSales" failed on SQL Server 2025 target VM (10.10.0.5) because the restore operator rejected the attempt, reflecting a mismatch in the target database context. The error indicates that the previously confirmed target database was "Sales," not "EnterpriseSales." Therefore, the system rejected restoring "EnterpriseSales" without explicit confirmation.

## Root Cause Analysis
- The restore automation maintains a previous restore context that points to the database "Sales."
- The current RESTORE request references "EnterpriseSales," causing a mismatch.
- This mismatch triggers the operator logic to reject the restore due to inconsistency in restore workflows.
- Potential contributing factors include:
  - Naming convention disparity between source database names and backup file discovery.
  - Automation tooling likely uses discovered filenames or metadata in the blob container `backupfromazsqlmi` under storage account `2276259blob`.
  - The restore is done using RESTORE FROM URL with SAS credentials, so if SAS tokens or blob listing failed, the operator might fallback or default target incorrectly, but that is not explicitly indicated here.
  - No direct indication that the .bak files for EnterpriseSales are missing or inaccessible in the container.
- The root cause is an operator or automation logic conflict due to a mismatch in expected target database name from previous context ("Sales") versus current requested target ("EnterpriseSales").

## Resolution Steps
1. **Confirm Restore Target Database:**
   - Verify whether the intended restore target is indeed "EnterpriseSales" or "Sales."
   - If "Sales" is intended, update the restore request accordingly.
   - If "EnterpriseSales" is intended, ensure no conflicting restore context remains from previous operations.

2. **Clear or Reset Restore Context:**
   - On the Linux dev VM automation environment, clear any cached or saved state that records the previous restore target database.
   - Restart the restore tool or script to reset internal state.

3. **Validate Backup File Availability:**
   - Use Azure CLI or Azure Portal to list blobs in container `backupfromazsqlmi` on storage account `2276259blob`:
     ```
     az storage blob list --account-name 2276259blob --container-name backupfromazsqlmi --output table --sas-token "<SAS_TOKEN>"
     ```
   - Confirm presence of the correct ".bak" files for "EnterpriseSales."
   - If mismatch in file names is found, align naming conventions or update automation discovery logic.

4. **Verify SAS Token Validity:**
   - Confirm SAS token used for RESTORE FROM URL has not expired and has sufficient permissions to read blobs.
   - Renew SAS token if expired.

5. **Validate Network and Firewall Settings:**
   - Confirm Linux dev VM and SQL Server 2025 VM can access Azure Blob Storage endpoints.
   - Test connectivity via curl or az storage blob commands.

6. **Perform Manual Restore Command Test:**
   - On the SQL Server 2025 VM, attempt a manual RESTORE FROM URL command referencing the EnterpriseSales backup file and the SAS URL to verify restore feasibility.

7. **Rerun Automation with Confirmed Parameters:**
   - After confirming correct database name and backup file, rerun the restore automation with the corrected database target and reset context.

## Prevention
- Implement automation logic to validate and confirm database target name consistency before issuing RESTORE commands.
- Add pre-restore checks that align restore target DB names with previous context or explicitly reset context between restores.
- Enforce strict naming conventions for backups and metadata in Azure Blob Storage to reduce ambiguity.
- Automate SAS token renewal and validation before starting backup or restore operations.
- Centralize logging for restore operations with clear state information to detect and avoid context mismatches.

## Suggested Tool Improvement
- Enhance the `restore_database` tool to:
  - Automatically detect and resolve conflicts in target database names between previous and current restore operations.
  - Provide an interactive confirmation prompt or explicit override flag to proceed when discrepancies arise.
  - Implement clearer error messages that guide operators on how to resolve mismatched target database names.
  - Include validation that backup files in the blob container correspond exactly to the requested database before issuing restore.
  - Log and track restore context states with timestamps for auditing and tracing restore sequences.
```