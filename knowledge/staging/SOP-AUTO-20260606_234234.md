# SOP-AUTO-2026-06-06T23:41:57 : Restore Failure Due to Database Name Mismatch and Validation Rejection

## Classification
- **Category**: RESTORE  
- **Severity**: P2  
- **Auto-Generated**: Yes  
- **Generated On**: 2026-06-06T23:41:57  

## Problem Description
The restore operation for the database `EnterpriseFinance` failed. The error message indicates a mismatch between the database name in the restore request (`EnterpriseFinance`) and the pre-check or triage context which references a different database name `Finance`. The system rejected the restore request because of this discrepancy, likely due to missing or inadequate validation/pre-checks for `EnterpriseFinance`. This prevented the restore tool from proceeding.

## Root Cause Analysis
- The **restore process** uses an automated tool (`restore_database`) running remotely on a Linux VM that connects to the target SQL Server instance (2025 on Windows, IP 10.10.0.5) using pyodbc.
- Backups reside in Azure Blob storage (container `backupfromazsqlmi` in storage account `2276259blob`) as `.bak` files, and restores are performed using the SQL Server `RESTORE FROM URL` method with SAS credentials.
- The error suggests either:
  - A **naming mismatch** between the source database name (`EnterpriseFinance`) and the backup file naming conventions or the restore pre-check mechanism is configured for a different database (`Finance`).
  - The validation or pre-check logic in the automation tool does not recognize or has no corresponding pre-check configuration for `EnterpriseFinance`.
  - The operation may have used an outdated or incorrect backup file listing from the blob container, potentially due to inconsistent naming or failure to synchronize the naming mapping.
- Other architectural possibilities like **missing `.bak` file**, **expired SAS token**, or **network/firewall issues** are unlikely as the error specifically highlights a mismatch and rejection related to database name validation rather than file or connectivity errors.
- The automation tool’s reliance on **blob listing** must correctly correlate backup files to database names, or else the restore request validation will fail.
- The referenced pre-check context for `Finance` database instead of `EnterpriseFinance` indicates:
  - Potential misconfiguration or legacy settings in the tool.
  - Backup `.bak` files or metadata for `EnterpriseFinance` may not be properly tagged or named in blob storage.
  
## Resolution Steps
1. **Verify Backup File Naming and Presence**
   - Log into Azure portal or use Azure CLI to list blobs in the container:  
     ```bash
     az storage blob list \
       --account-name 2276259blob \
       --container-name backupfromazsqlmi \
       --query "[?contains(name, 'EnterpriseFinance')]" \
       --output table
     ```
   - Confirm that `.bak` backup files for `EnterpriseFinance` exist and are named consistently with the expected pattern used by the restore tool.

2. **Check SAS Token Validity and Permissions**
   - Validate that the SAS token used by the automation tool is active and grants Read/List permissions on the blob container.
   - Renew SAS token if expired or permissions insufficient.

3. **Review Automation Tool Configuration**
   - Inspect the `restore_database` script or process to:
     - Ensure the database name parameter is correctly set to `EnterpriseFinance`.
     - Verify that the pre-check module or validation logic supports the `EnterpriseFinance` database.
     - Update or add validation rules for `EnterpriseFinance` if missing.
     - Confirm that the blob listing logic correctly filters and matches backup files for this specific database.

4. **Perform a Dry Run of the Restore Using Validated Backup URL**
   - Construct a RESTORE command within SQL Server 2025 VM using a valid SAS URL to a known backup file for `EnterpriseFinance`:  
     ```sql
     RESTORE DATABASE EnterpriseFinance
     FROM URL = 'https://2276259blob.blob.core.windows.net/backupfromazsqlmi/EnterpriseFinance_backup_YYYYMMDD.bak'
     WITH 
       CREDENTIAL = 'RestoreSASCredential',
       REPLACE,
       STATS = 10;
     ```
   - Run this manually to confirm the database restores without errors.

5. **Update Documentation and Communicate**
   - Document the fix and configuration changes in the tool.
   - Inform stakeholders of the resolved issue and preventive measures taken.

## Prevention
- Implement strict naming conventions for backup files that include the exact database name to avoid ambiguity.
- Enhance automation pre-check validation to dynamically discover and support new databases without human intervention.
- Schedule periodic validation of SAS tokens and maintain automated renewal workflows.
- Monitor blob storage access logs and backup file presence regularly to ensure backups are in place.
- Automate sanity checks of restore scripts and their parameter mappings during CI/CD deployments.

## Suggested Tool Improvement
- Include dynamic database name discovery and validation against blob contents to avoid hardcoded or legacy database name checks.
- Add improved error messages showing exact conflicting names and suggestions for correcting mismatches.
- Implement automatic refresh and caching of blob lists to ensure backup metadata is up-to-date.
- Integrate SAS token validity checks before restore attempts and trigger automatic renewal or alerts if tokens are near expiration.
- Provide a dry-run or validation mode in the `restore_database` tool for pre-validating all parameters including database name, backup file presence, and SAS accessibility.