```markdown
# SOP-AUTO-2026-06-08T183534 : Restore Failure due to Unavailable Disk Space Verification on Target VM

## Classification
- **Category**: DISK
- **Severity**: P1
- **Auto-Generated**: Yes
- **Generated On**: 2026-06-08T18:35:34

## Problem Description
A restore operation of the database "EnterpriseSales" from Azure SQL Managed Instance to a SQL Server 2025 instance running on a Windows VM at IP 10.10.0.5 has failed to proceed. The failure is due to inability to verify if the target VM has sufficient disk space available before the restore. The disk space check service is currently unavailable, halting the restore process as per SOP-RESTORE-001 requirements.

## Root Cause Analysis
Given the architecture and failure context:

- The restore uses RESTORE FROM URL with SAS-based access directly to Azure Blob Storage (container: `backupfromazsqlmi` in `2276259blob` storage account).  
- The backups exist in blob storage (confirmed by automation listing backups).
- The target VM "10.10.0.5" hosts SQL Server 2025 with "EnterpriseSales" database online and no active connections, meeting all readiness criteria except disk space check.  
- The disk space verification relies on an external "check_disk_space" service which is currently unavailable, preventing validation of the available free space on the VM.  
- Without confirmed disk space, SOP-RESTORE-001 disallows restore to avoid potential failure or VM disk full errors during restore.  
- Network access and storage permissions appear intact given no reported problems listing the blob container or reading the SAS token.  
- No evidence indicates backup file absence, SAS expiry, or naming mismatches.  
- The key blocker remains the unavailability of the disk space check service, a prerequisite safety check per SOP.

## Resolution Steps
1. **Manually Check Disk Space on Target VM:**
   - Remotely connect to the Windows VM (`10.10.0.5`) using RDP or PowerShell Remoting.
   - Run `Get-PSDrive` or `dir` on the disk hosting the SQL Server database files to collect the current free disk space.
   - Confirm that free space is at least 1.5 times the backup file size (backup file size can be obtained by listing blobs in container `backupfromazsqlmi` via Azure CLI or portal).

2. **Temporarily Bypass the Automated Disk Check:**
   - If manual verification confirms sufficient disk space, proceed with the restore by instructing the restore automation to override the disk check step if possible.
   - Document the manual verification in restore logs for audit.

3. **Restore Database from URL with SAS Token:**
   - Use RESTORE DATABASE `EnterpriseSales` FROM URL = '<backup .bak file URL with SAS token>' WITH appropriate MOVE and RECOVERY options.
   - Ensure restore uses the correct blob URL and valid SAS token.

4. **Post-Restore Validation:**
   - Set `EnterpriseSales` database to MULTI_USER mode.
   - Fix orphaned users, update statistics, and apply any required data masking.
   - Verify database accessibility and integrity.

5. **Restore Disk Space Check Service:**
   - Investigate and resolve the service unavailability preventing automated disk space checks.
   - Check for service crashes, network connectivity issues, or permission problems.
   - Restart or redeploy the disk space check agent/service on the target VM as needed.

## Prevention
- Implement monitoring and alerting on all essential verification services, including disk space check, to prevent unexpected unavailability.
- Add a fallback procedure allowing manual disk space verification with documented override in automation for emergency cases.
- Regularly test and update SAS tokens and their expiry dates used for backup blob access.
- Ensure consistent backup file naming conventions and blob container paths matching the source database names.
- Validate network connectivity and firewall rules between automation VM and target VM continuously.

## Suggested Tool Improvement
- Enhance restore automation to:
  - Detect when the disk space check service is unavailable and prompt for manual input or fallback verification.
  - Cache last known good disk space info with timestamp to allow limited tolerance during service outages.
  - Provide a UI or CLI flag to allow manual override of disk space checks after administrator confirmation.
  - Implement detailed error logging including SAS token expiry and blob path validation status.
  - Auto-validate active connections and database states more robustly before restoring.
```